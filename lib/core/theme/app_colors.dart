import 'package:flutter/material.dart';
import 'package:sanitarypad/core/theme/app_theme.dart';

class AppColors {
  // Brand Colors
  static const Color primary = AppTheme.primaryPink;
  static const Color secondary = AppTheme.lavender;
  static const Color accent = AppTheme.accentCoral;

  // Neutral Colors
  static const Color background = AppTheme.splashLight;
  static const Color surface = Colors.white;
  static const Color textPrimary = AppTheme.darkGray;
  static const Color textSecondary = AppTheme.mediumGray;

  // Semantic
  static const Color success = AppTheme.successGreen;
  static const Color error = AppTheme.errorRed;
  static const Color warning = AppTheme.warningOrange;
  static const Color info = AppTheme.infoBlue;
}
